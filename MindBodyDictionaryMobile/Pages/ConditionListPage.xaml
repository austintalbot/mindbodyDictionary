<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:MindBodyDictionaryMobile.PageModels"
             xmlns:models="clr-namespace:MindBodyDictionaryMobile.Models"
             xmlns:controls="clr-namespace:MindBodyDictionaryMobile.Pages.Controls"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="MindBodyDictionaryMobile.Pages.ConditionListPage"
             x:Name="ConditionList"
             x:DataType="pageModels:ConditionListPageModel"
             Title="Conditions">


    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
            EventName="Appearing"
            BindingContext="{Binding Path=BindingContext, Source={x:Reference ConditionList}, x:DataType=ContentPage}"
            Command="{Binding AppearingCommand}"/>
    </ContentPage.Behaviors>
    <Grid>
      <ScrollView>
        <VerticalStackLayout
            BindableLayout.ItemsSource="{Binding Conditions}"
            Margin="{StaticResource LayoutPadding}"
            Spacing="{StaticResource LayoutSpacing}">
            <BindableLayout.ItemTemplate>
                <DataTemplate x:DataType="models:Condition">
                    <Border SemanticProperties.Description="{Binding Name, StringFormat='{0} Condition'}">
                        <VerticalStackLayout Padding="10">
                            <Label Text="{Binding Name}" FontSize="24" />
                            <Label Text="{Binding Description}" />
                        </VerticalStackLayout>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding NavigateToConditionCommand, Source={RelativeSource AncestorType={x:Type pageModels:ConditionListPageModel}}, x:DataType=pageModels:ConditionListPageModel}" CommandParameter="{Binding .}"/>
                        </Border.GestureRecognizers>
                    </Border>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </VerticalStackLayout>
      </ScrollView>

        <controls:AddButton
            Command="{Binding AddConditionCommand}"
            SemanticProperties.Description="Add condition" />
    </Grid>
</ContentPage>
