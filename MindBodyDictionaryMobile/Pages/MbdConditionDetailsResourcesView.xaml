<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="MindBodyDictionaryMobile.Pages.MbdConditionDetailsResourcesView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:MindBodyDictionaryMobile.Models"
    xmlns:vm="clr-namespace:MindBodyDictionaryMobile.PageModels"
    x:Name="this"
    x:DataType="vm:MbdConditionDetailPageModel">
    <Grid RowDefinitions="*">
        <CollectionView
            x:Name="ResourceCollectionView"
            x:DataType="vm:MbdConditionDetailPageModel"
            EmptyView="There are no corresponding resources for this Condition"
            InputTransparent="False"
            ItemsSource="{Binding BooksResourcesList}"
            SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid
                        x:DataType="model:Recommendation"
                        HorizontalOptions="Center"
                        InputTransparent="False"
                        VerticalOptions="Fill">
                        <Border
                            Background="White"
                            InputTransparent="False"
                            Style="{StaticResource RecommendationBorder}">
                            <Grid RowDefinitions="Auto,Auto,Auto">
                                <Label
                                    Grid.Row="0"
                                    Style="{StaticResource RecommendationNameLabel}"
                                    Text="{Binding Name}" />
                                <BoxView Grid.Row="1" Style="{StaticResource RecommendationBoxView}" />
                                <Button
                                    Grid.Row="2"
                                    Margin="0,10,0,0"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:RecommendationsPageModel}}, Path=AddToMyListCommand}"
                                    CommandParameter="{Binding .}"
                                    Style="{StaticResource AccentButton}"
                                    Text="Add to List" />
                            </Grid>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MbdConditionDetailPageModel}}, Path=ResourceLinkClickCommand}" CommandParameter="{Binding .}" />
                            </Border.GestureRecognizers>
                        </Border>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentView>
