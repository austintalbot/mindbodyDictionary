<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:f="clr-namespace:Fonts"
             xmlns:converters="clr-namespace:MindBodyDictionaryMobile.Converter"
             x:Class="MindBodyDictionaryMobile.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
                <ResourceDictionary Source="Resources/Styles/AppStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <converters:IsNotNullOrEmptyConverter x:Key="IsNotNullOrEmptyConverter" />
            <converters:BoolToRegisteredTextConverter x:Key="BoolToRegisteredTextConverter" />
            <converters:BoolToColorConverter x:Key="BoolToColorConverter" />
            <converters:BoolToTextConverter x:Key="BoolToTextConverter" />
            <converters:InverseBoolConverter x:Key="InverseBoolConverter" />

            <!-- App-specific Colors -->
            <Color x:Key="ActionColor">#00606E</Color>
            <Color x:Key="PrimaryColor">#6B329B</Color>
            <Color x:Key="HeaderColor">#4A1085</Color>
            <Color x:Key="PrimaryColorDark">#4A1085</Color>
            <Color x:Key="BackgroundColor">#FFFFFF</Color>
            <Color x:Key="GoColor">#4F794E</Color>
            <Color x:Key="BarColor">#00606E</Color>
            <Color x:Key="BarSelectedColor">#73ABC9</Color>
            <Color x:Key="MbdConditionCardColor">#B4B1AF</Color>
            <Color x:Key="ButtonColor">#00606E</Color>
            <Color x:Key="MbdConditionTextColor">#00606E</Color>
            <Color x:Key="MbdConditionCardBackground">#FFFFFF</Color>
            <Color x:Key="MbdConditionCardBorder">#6A7D95</Color>
            <Color x:Key="MbdConditionDetailCardBackground">#00606E</Color>
            <Color x:Key="MbdConditionDetailCardText">#FFFFFF</Color>
            <Color x:Key="MbdConditionDetailCardBorder">#73ABC9</Color>
            <Color x:Key="LightIconColor">black</Color>
            <Color x:Key="DarkIconColor">white</Color>

            <FontImageSource x:Key="IconLight"
                Glyph="{x:Static f:FluentUI.weather_sunny_28_regular}"
                FontFamily="{x:Static f:FluentUI.FontFamily}"
                Color="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}"
                Size="{StaticResource size240}" />
            <FontImageSource x:Key="IconDark"
                Glyph="{x:Static f:FluentUI.weather_moon_28_regular}"
                FontFamily="{x:Static f:FluentUI.FontFamily}"
                Color="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}"
                Size="{StaticResource size240}" />

            <!-- Main Page Layout Styles -->
            <Style x:Key="MainPageGrid" TargetType="Grid">
                <Setter Property="RowDefinitions" Value="*,Auto" />
                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource OffBlack}}" />
            </Style>

            <Style x:Key="ContentScrollView" TargetType="ScrollView">
                <Setter Property="VerticalOptions" Value="Fill" />
                <Setter Property="HorizontalOptions" Value="Fill" />
            </Style>

            <Style x:Key="MainContentStack" TargetType="VerticalStackLayout">
                <Setter Property="Spacing" Value="20" />
                <Setter Property="Margin" Value="20" />
                <Setter Property="VerticalOptions" Value="Fill" />
            </Style>

            <!-- Logo and Image Styles -->
            <Style x:Key="AppLogoImage" TargetType="Image">
                <Setter Property="Source" Value="mbd_logo.png" />
                <Setter Property="HeightRequest" Value="180" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="Aspect" Value="AspectFit" />
                <Setter Property="SemanticProperties.Description" Value="Mind Body Dictionary Logo" />
            </Style>

            <Style x:Key="AppIconImage" TargetType="Image">
                <Setter Property="Source" Value="mbd_icon.png" />
                <Setter Property="HeightRequest" Value="120" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="Aspect" Value="AspectFit" />
                <Setter Property="SemanticProperties.Description" Value="Mind Body Dictionary Icon" />
            </Style>

            <Style x:Key="SmallLogoImage" TargetType="Image">
                <Setter Property="Source" Value="mbd_logo.png" />
                <Setter Property="HeightRequest" Value="150" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="Aspect" Value="AspectFit" />
                <Setter Property="SemanticProperties.Description" Value="Mind Body Dictionary Logo" />
            </Style>

            <!-- Label Styles -->
            <Style x:Key="PageTitleLabel" TargetType="Label">
                <Setter Property="FontSize" Value="30" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="TextColor" Value="{StaticResource ActionColor}" />
                <Setter Property="Margin" Value="10,0,10,5" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style x:Key="SectionTitleLabel" TargetType="Label">
                <Setter Property="FontSize" Value="24" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="TextColor" Value="{StaticResource MbdConditionTextColor}" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style x:Key="SubtitleLabel" TargetType="Label">
                <Setter Property="FontSize" Value="18" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="TextColor" Value="{StaticResource MbdConditionTextColor}" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style x:Key="BodyTextLabel" TargetType="Label">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
                <Setter Property="LineBreakMode" Value="WordWrap" />
            </Style>

            <Style x:Key="ParagraphLabel" TargetType="Label">
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="FontSize" Value="22" />
                <Setter Property="Margin" Value="10,8" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="TextColor" Value="{StaticResource MbdConditionTextColor}" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style x:Key="AboutParagraphLabel" TargetType="Label" BasedOn="{StaticResource ParagraphLabel}">
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="TextColor" Value="{StaticResource ActionColor}" />
            </Style>

            <Style x:Key="VersionLabel" TargetType="Label">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="TextColor" Value="{StaticResource Gray400}" />
                <Setter Property="Margin" Value="0" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="HorizontalOptions" Value="Start" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style x:Key="AboutTitleLabel" TargetType="Label">
                <Setter Property="FontSize" Value="22" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style x:Key="AboutBodyLabel" TargetType="Label">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="Padding" Value="15" />
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
                <Setter Property="LineBreakMode" Value="WordWrap" />
                <Setter Property="HorizontalTextAlignment" Value="Start" />
            </Style>

            <Style x:Key="ContentPageGrid" TargetType="Grid">
                <Setter Property="RowDefinitions" Value="*,Auto" />
            </Style>

            <!-- Button Styles -->
            <Style x:Key="PrimaryButton" TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource ButtonColor}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="HorizontalOptions" Value="Fill" />
                <Setter Property="Margin" Value="10" />
                <Setter Property="MinimumHeightRequest" Value="44" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style x:Key="RandomMbdConditionButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                <Setter Property="Margin" Value="0" />
            </Style>

            <Style x:Key="SubmitButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                <Setter Property="VerticalOptions" Value="End" />
            </Style>

            <!-- SearchBar Styles -->
            <Style x:Key="MbdConditionSearchBar" TargetType="SearchBar">
                <Setter Property="Placeholder" Value="Search conditions..." />
                <Setter Property="CancelButtonColor" Value="{StaticResource PrimaryColor}" />
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource PrimaryColor}, Dark=White}" />
                <Setter Property="PlaceholderColor" Value="{AppThemeBinding Light={StaticResource PrimaryColor}, Dark=White}" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="FontSize" Value="Medium" />
                <Setter Property="Background" Value="{AppThemeBinding Dark=Gray, Light=White}" />
                <Setter Property="FontAttributes" Value="Italic" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
                <Setter Property="Margin" Value="10,10,10,5" />
            </Style>

            <!-- Card and Border Styles -->
            <Style x:Key="MbdConditionCard" TargetType="Border">
                <Setter Property="StrokeShape" Value="RoundRectangle,12" />
                <Setter Property="Stroke" Value="{StaticResource MbdConditionCardBorder}" />
                <Setter Property="StrokeThickness" Value="1" />
                <Setter Property="BackgroundColor" Value="{StaticResource MbdConditionCardBackground}" />
                <Setter Property="Padding" Value="15" />
                <Setter Property="Margin" Value="10" />
            </Style>

            <Style x:Key="MbdConditionCardWithShadow" TargetType="Border" BasedOn="{StaticResource MbdConditionCard}">
                <Setter Property="Shadow">
                    <Shadow Brush="Black" Opacity="0.1" Radius="10" Offset="0,2"/>
                </Setter>
            </Style>

            <Style x:Key="RecommendationBorder" TargetType="Border">
                <Setter Property="Margin" Value="10" />
                <Setter Property="Padding" Value="10" />
                <Setter Property="BackgroundColor" Value="{StaticResource MbdConditionDetailCardBackground}" />
                <Setter Property="Stroke" Value="{StaticResource MbdConditionDetailCardBorder}" />
                <Setter Property="StrokeThickness" Value="1" />
                <Setter Property="StrokeShape" Value="RoundRectangle 12" />
            </Style>

            <Style x:Key="InfoCardBorder" TargetType="Border">
                <Setter Property="BackgroundColor" Value="LightGray" />
                <Setter Property="Stroke" Value="Gray" />
                <Setter Property="StrokeThickness" Value="1" />
                <Setter Property="Padding" Value="15" />
                <Setter Property="StrokeShape" Value="RoundRectangle 5" />
                <Setter Property="Shadow">
                    <Shadow Brush="Black" Opacity="0.1" Radius="5" Offset="0,2"/>
                </Setter>
            </Style>

            <Style x:Key="HighlightCardBorder" TargetType="Border">
                <Setter Property="BackgroundColor" Value="LightBlue" />
                <Setter Property="Stroke" Value="Blue" />
                <Setter Property="StrokeThickness" Value="1" />
                <Setter Property="Padding" Value="15" />
                <Setter Property="StrokeShape" Value="RoundRectangle 5" />
                <Setter Property="Shadow">
                    <Shadow Brush="Black" Opacity="0.1" Radius="5" Offset="0,2"/>
                </Setter>
            </Style>

            <!-- Recommendation Styles -->
            <Style x:Key="RecommendationNameLabel" TargetType="Label">
                <Setter Property="FontSize" Value="24" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="{StaticResource MbdConditionDetailCardText}" />
                <Setter Property="VerticalOptions" Value="Start" />
                <Setter Property="VerticalTextAlignment" Value="Center" />
                <Setter Property="Text" Value="{Binding Name}" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style x:Key="RecommendationDescriptionLabel" TargetType="Label">
                <Setter Property="FontSize" Value="18" />
                <Setter Property="Margin" Value="30,0" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="TextColor" Value="{StaticResource MbdConditionDetailCardText}" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="VerticalTextAlignment" Value="Center" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style x:Key="RecommendationBookProductDescriptionLabel" TargetType="Label" BasedOn="{StaticResource RecommendationDescriptionLabel}">
                <Setter Property="Text" Value="Click to learn more about the health benefits of this product" />
            </Style>

            <Style x:Key="RecommendationClickLabel" TargetType="Label">
                <Setter Property="FontSize" Value="15" />
                <Setter Property="TextColor" Value="{StaticResource White}" />
                <Setter Property="VerticalOptions" Value="End" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="Text" Value="Tap to Learn More" />
                <Setter Property="FontAttributes" Value="Italic" />
                <Setter Property="MaxLines" Value="1" />
                <Setter Property="LineBreakMode" Value="TailTruncation" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style x:Key="RecommendationBookProductClickLabel" TargetType="Label" BasedOn="{StaticResource RecommendationClickLabel}">
                <Setter Property="Text" Value="We applaud quality products" />
            </Style>

            <Style x:Key="RecommendationBoxView" TargetType="BoxView">
                <Setter Property="BackgroundColor" Value="{StaticResource ActionColor}" />
                <Setter Property="HeightRequest" Value="2" />
                <Setter Property="HorizontalOptions" Value="Fill" />
                <Setter Property="VerticalOptions" Value="Start" />
            </Style>

            <!-- Food Item Styles -->
            <Style x:Key="FoodItemBorder" TargetType="Border">
                <Setter Property="Padding" Value="5" />
                <Setter Property="Margin" Value="2" />
                <Setter Property="BackgroundColor" Value="{StaticResource ActionColor}" />
                <Setter Property="StrokeShape" Value="RoundRectangle 12" />
                <Setter Property="VerticalOptions" Value="Fill" />
                <Setter Property="HorizontalOptions" Value="Fill" />
                <Setter Property="StrokeThickness" Value="0" />
            </Style>

            <Style x:Key="FoodItemLabel" TargetType="Label">
                <Setter Property="BackgroundColor" Value="{StaticResource ActionColor}" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="HorizontalOptions" Value="Start" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
                <Setter Property="VerticalTextAlignment" Value="Center" />
            </Style>

            <!-- Collection View Styles -->
            <Style x:Key="SearchGridCollectionView" TargetType="CollectionView">
                <Setter Property="SelectionMode" Value="Single" />
                <Setter Property="ItemSizingStrategy" Value="MeasureAllItems" />
                <Setter Property="RemainingItemsThreshold" Value="5" />
                <Setter Property="Margin" Value="10,5,10,5" />
            </Style>

            <Style x:Key="GridItemsLayoutThreeColumn" TargetType="GridItemsLayout">
                <Setter Property="Span" Value="3" />
                <Setter Property="VerticalItemSpacing" Value="8" />
                <Setter Property="HorizontalItemSpacing" Value="8" />
            </Style>

            <Style x:Key="SearchMbdConditionCard" TargetType="Border">
                <Setter Property="HeightRequest" Value="140" />
                <Setter Property="Stroke" Value="{StaticResource MbdConditionCardBorder}" />
                <Setter Property="BackgroundColor" Value="{StaticResource MbdConditionCardBackground}" />
                <Setter Property="StrokeThickness" Value="1" />
                <Setter Property="Padding" Value="6" />
                <Setter Property="StrokeShape" Value="RoundRectangle 8" />
            </Style>

            <Style x:Key="SearchMbdConditionImage" TargetType="Image">
                <Setter Property="HeightRequest" Value="50" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="WidthRequest" Value="50" />
                <Setter Property="Aspect" Value="AspectFill" />
            </Style>

            <Style x:Key="SearchMbdConditionLabel" TargetType="Label">
                <Setter Property="FontSize" Value="11" />
                <Setter Property="MaxLines" Value="2" />
                <Setter Property="TextColor" Value="{StaticResource MbdConditionTextColor}" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="LineBreakMode" Value="TailTruncation" />
                <Setter Property="Margin" Value="0,4,0,0" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style x:Key="EmptyViewLabel" TargetType="Label">
                <Setter Property="FontAttributes" Value="Italic" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style x:Key="EmptyViewTitleLabel" TargetType="Label" BasedOn="{StaticResource EmptyViewLabel}">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="TextColor" Value="{StaticResource Gray600}" />
                <Setter Property="Text" Value="No conditions available" />
            </Style>

            <Style x:Key="EmptyViewSubtitleLabel" TargetType="Label" BasedOn="{StaticResource EmptyViewLabel}">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="TextColor" Value="{StaticResource Gray400}" />
                <Setter Property="Text" Value="Pull to refresh" />
            </Style>

            <Style x:Key="ShimmerPlaceholderBorder" TargetType="Border">
                <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}" />
                <Setter Property="Opacity" Value="0.7" />
                <Setter Property="StrokeShape" Value="RoundRectangle 6" />
            </Style>

            <Style x:Key="ShimmerTextLineBorder" TargetType="Border" BasedOn="{StaticResource ShimmerPlaceholderBorder}">
                <Setter Property="HeightRequest" Value="20" />
                <Setter Property="Opacity" Value="0.6" />
                <Setter Property="StrokeShape" Value="RoundRectangle 3" />
            </Style>

            <Style x:Key="ShimmerTitleBorder" TargetType="Border" BasedOn="{StaticResource ShimmerPlaceholderBorder}">
                <Setter Property="WidthRequest" Value="130" />
                <Setter Property="HeightRequest" Value="18" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="Margin" Value="0,10,0,0" />
                <Setter Property="Opacity" Value="0.6" />
                <Setter Property="StrokeShape" Value="RoundRectangle 3" />
            </Style>

            <!-- Ailment Summary Page Styles -->
            <Style x:Key="MbdConditionSummaryGrid" TargetType="Grid">
                <Setter Property="Margin" Value="10,15" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="RowDefinitions" Value="Auto,Auto,*,4*,*,*" />
                <Setter Property="ColumnDefinitions" Value="50,*,50" />
            </Style>

            <Style x:Key="MbdConditionImageBorder" TargetType="Border">
                <Setter Property="BackgroundColor" Value="{StaticResource MbdConditionCardBackground}" />
                <Setter Property="Stroke" Value="{StaticResource MbdConditionCardBorder}" />
                <Setter Property="StrokeThickness" Value="1" />
                <Setter Property="StrokeShape" Value="RoundRectangle 8" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="Padding" Value="10" />
            </Style>

            <Style x:Key="MbdConditionSummaryImage" TargetType="Image">
                <Setter Property="HeightRequest" Value="175" />
                <Setter Property="WidthRequest" Value="175" />
                <Setter Property="Aspect" Value="AspectFit" />
                <Setter Property="HorizontalOptions" Value="Center" />
            </Style>

            <Style x:Key="MindsetLabel" TargetType="Label">
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="TextColor" Value="{StaticResource MbdConditionTextColor}" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
                <Setter Property="FontAttributes" Value="Italic" />
                <Setter Property="Margin" Value="0,10,0,5" />
            </Style>

            <Style x:Key="MbdConditionNameLabel" TargetType="Label">
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="FontSize" Value="24" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="{StaticResource MbdConditionTextColor}" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
                <Setter Property="Padding" Value="0,0,0,10" />
            </Style>

            <Style x:Key="SummaryScrollView" TargetType="ScrollView">
                <Setter Property="VerticalScrollBarVisibility" Value="Always" />
                <Setter Property="VerticalOptions" Value="Start" />
                <Setter Property="HorizontalOptions" Value="Fill" />
            </Style>

            <Style x:Key="SummaryLabel" TargetType="Label">
                <Setter Property="FontSize" Value="20" />
                <Setter Property="LineHeight" Value="1.2" />
                <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Black}, Dark={StaticResource White}}" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style x:Key="CopyrightLabel" TargetType="Label">
                <Setter Property="FontSize" Value="13" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="VerticalTextAlignment" Value="Center" />
                <Setter Property="TextColor" Value="{StaticResource Gray400}" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
            </Style>

            <Style x:Key="ShareButton" TargetType="Image">
                <Setter Property="Aspect" Value="AspectFit" />
                <Setter Property="HeightRequest" Value="36" />
                <Setter Property="WidthRequest" Value="36" />
                <Setter Property="HorizontalOptions" Value="End" />
                <Setter Property="VerticalOptions" Value="Center" />
            </Style>

            <Style x:Key="StandardCollectionView" TargetType="CollectionView">
                <Setter Property="SelectionMode" Value="None" />
                <Setter Property="VerticalOptions" Value="Fill" />
                <Setter Property="HorizontalOptions" Value="Fill" />
            </Style>

            <Style x:Key="HorizontalCollectionView" TargetType="CollectionView" BasedOn="{StaticResource StandardCollectionView}">
                <Setter Property="ItemsLayout" Value="HorizontalList" />
                <Setter Property="HeightRequest" Value="300" />
            </Style>

            <Style x:Key="VerticalCollectionView" TargetType="CollectionView" BasedOn="{StaticResource StandardCollectionView}">
                <Setter Property="ItemsLayout" Value="VerticalList" />
                <Setter Property="Margin" Value="10" />
            </Style>

            <!-- Layout Container Styles -->
            <Style x:Key="HeaderSection" TargetType="VerticalStackLayout">
                <Setter Property="Spacing" Value="8" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="Margin" Value="0,0,0,10" />
            </Style>

            <Style x:Key="ContentSection" TargetType="VerticalStackLayout">
                <Setter Property="Spacing" Value="15" />
                <Setter Property="Margin" Value="10" />
            </Style>

            <Style x:Key="ButtonSection" TargetType="VerticalStackLayout">
                <Setter Property="Spacing" Value="15" />
                <Setter Property="Margin" Value="5" />
                <Setter Property="VerticalOptions" Value="Start" />
            </Style>

            <!-- Grid Styles -->
            <Style x:Key="ContentGrid" TargetType="Grid">
                <Setter Property="Padding" Value="0" />
                <Setter Property="VerticalOptions" Value="Fill" />
                <Setter Property="HorizontalOptions" Value="Fill" />
            </Style>

            <Style x:Key="TabContentGrid" TargetType="Grid" BasedOn="{StaticResource ContentGrid}">
                <Setter Property="RowDefinitions" Value="*" />
                <Setter Property="Margin" Value="10" />
            </Style>

            <!-- Progress and Loading Styles -->
            <Style x:Key="LoadingProgressBar" TargetType="ProgressBar">
                <Setter Property="HeightRequest" Value="10" />
                <Setter Property="ProgressColor" Value="{StaticResource Primary}" />
            </Style>

            <!-- Subscribe/Lock Overlay Styles -->
            <Style x:Key="LockOverlayStack" TargetType="VerticalStackLayout">
                <Setter Property="BackgroundColor" Value="LightGray" />
                <Setter Property="Opacity" Value="0.93" />
                <Setter Property="InputTransparent" Value="False" />
                <Setter Property="VerticalOptions" Value="Fill" />
            </Style>

            <Style x:Key="LockIcon" TargetType="Image">
                <Setter Property="HeightRequest" Value="100" />
                <Setter Property="WidthRequest" Value="100" />
                <Setter Property="Margin" Value="20" />
                <Setter Property="Source" Value="locked_icon.png" />
                <Setter Property="VerticalOptions" Value="Center" />
            </Style>

            <Style x:Key="LockMessageLabel" TargetType="Label">
                <Setter Property="FontAttributes" Value="Italic" />
                <Setter Property="FontSize" Value="20" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="FontFamily" Value="OpenSansRegular" />
                <Setter Property="Margin" Value="10,8" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="TextColor" Value="{StaticResource MbdConditionTextColor}" />
                <Setter Property="Text" Value="Subscribe to remove ads and gain access to this and more content!" />
                <Setter Property="VerticalOptions" Value="Center" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>