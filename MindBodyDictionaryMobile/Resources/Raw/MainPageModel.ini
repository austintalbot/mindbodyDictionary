MainPageModel.Appearing (first run only)
  → InitData checks is_seeded preference
    → LoadSeedDataAsync clears tables
      → SeedConditionsAsync
        → LoadConditionsFromLocalFileAsync
          → Read ConditionsSeedData.json
          → System.Text.Json deserialize (NOW WORKS with JsonPropertyName attributes)
          → List<MbdCondition> with 3 conditions
          → SaveConditionsToDatabase
            → ConditionRepository.SaveItemAsync for each condition
            → SQLite INSERT
        → ConditionListPageModel.Appearing
          → AppearingCommand (NOW FIRES with fixed binding)
          → ListAsync (WON'T CRASH with Tasks initialization)
          → Bind to Collection (XAML RECOGNIZES MbdCondition)
          → Display 3 seeded conditions on UI
          